# UNIVERSAL PROTOCOL STANDARD - ENVIRONMENT CONFIGURATION
# Copy to .env and set your values

# Database Configuration
DATABASE_URL=postgresql://...
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=rca_intelligence

# AI Service Encryption (REQUIRED - 32+ characters)
AI_KEY_ENCRYPTION_SECRET=your-32-character-encryption-secret-key-here-change-this

# Node Environment
NODE_ENV=development

# CRITICAL: NO hardcoded values allowed in code
# ALL configuration must be environment or database driven
# Run protocol_check.sh before committing